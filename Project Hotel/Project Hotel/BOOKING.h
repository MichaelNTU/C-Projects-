// This is the Booking.h file, it takes booking of Rooms, Menu and, then save it to a file

#pragma once
#include <string>
using namespace std;

class BOOKING
{
public:
	BOOKING(string, int, int counter);
	BOOKING();
	int getBookingFile();

private:


};